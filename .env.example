# Base URL of the SIE backend API.
# Local development default — copy this file to .env.local for local work.
# NOTE: VITE_* vars are inlined at build time, so the Render/Cloudflare Pages
# production URL is set only in the Pages build settings, never here.
VITE_API_URL=http://localhost:8080
